'''
# Exercise: 
1. Add a `__str__` method to the `Scoop` class, so that printing a scoop gives you something like `Scoop of FLAVOR`.
2. Add a `__str__` method to the `Bowl` class, so that printing a bowl shows something like this:
    Bowl of:
    - Scoop of chocolate
    - Scoop of vanilla
    - Scoop of coffee
'''